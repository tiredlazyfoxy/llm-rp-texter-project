# Step 003 — step-specific context

Feature-wide anchors (accept call sites, chain auto-commit buffering, reject-row
write location) are in `context.md` under "Feature-014 anchors this feature builds
on". D2 (auto-trigger gate) and D3/D4 are in "Locked design decisions".

Step-local facts:

- `_record_accept_and_retune` is at `chat_service.py:561`; its three callers are
  `chat_service.py:655` (explicit accept, `emitter=None`),
  `chain_generation_service.py:845` (auto-commit, `emitter=_emit`),
  `simple_generation_service.py:382` (auto-commit, `emitter=None`).
- The gate (D2) uses turn-scoped `feedback_db.list_by_turn(session_id,
  turn_number)` — this is the ONLY place the turn scope is used now; the retune
  evidence itself is session-wide (step 001). Do not confuse the two: gate =
  turn-scoped presence check; evidence = session-wide (inside the core).
- `chain_generation_service.py:826-856` — `_emit` appends `sse(name, payload)`
  strings to `pending_frames` when `caller_role != "player"`. Verify whether
  `pending_frames`/`_emit` carries anything other than `tuning_update`; the harvest
  indicates this buffer exists specifically to flush the post-accept
  `tuning_update`. If nothing else uses it, remove the whole buffer + flush loop.
- `sse(event, data)` frame formatter is `chat_agent_service.py:37`;
  `caller_role != "player"` is the editor-gate idiom (unchanged elsewhere).
- Fire-and-forget means: the scheduled task must not be awaited inside the request
  coroutine. `retune_tasks.start(...)` itself is `async` (it awaits the per-session
  lock + old-task teardown) but returns before the retune LLM runs — `await
  retune_tasks.start(...)` is correct and still non-blocking w.r.t. the LLM.
