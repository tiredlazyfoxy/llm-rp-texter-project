# Step 001 — step-specific context

Feature-wide anchors (retune_service internals, feedback DAL surface, prompt file
location) are in `context.md` under "Feature-014 anchors this feature builds on".

Step-local facts:

- `db/generation_feedback.py` public API today is exactly `create` (:9),
  `list_by_turn(session_id, turn_number)` (:18, `created_at` order),
  `delete_by_session(session_id)` (:29). Add `list_by_session(session_id)` as a
  fourth sibling, mirroring `list_by_turn`'s ordering but dropping the
  turn-number filter. No SQLModel table change → `db_import_export.py`
  `TABLE_REGISTRY` and `db/engine.py` imports are untouched (confirm they are not
  edited — outside this step's scope anyway).
- `retune_service.maybe_retune` (:55) currently reads `feedback_db.list_by_turn`;
  the partition-by-scope and `_retune_dimension` helpers already exist below it —
  reuse them, only the evidence source changes. `RetuneEmitter` alias is at :30.
- The turn gate (D2) is intentionally being REMOVED from the core here; step 003
  re-applies it at the accept call site. Do not add a `turn_number` gate inside the
  core. The core may still accept a `turn_number` argument if the prompt/logging
  wants it, but it must not use it to skip running.
- `_retune_dimension` issues one non-tool LLM completion via
  `get_llm_client_for_model(model_id)`; that internal is unchanged.
