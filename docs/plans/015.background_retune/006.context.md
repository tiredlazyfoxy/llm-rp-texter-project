# Step 006 — step-specific context

Feature-wide anchors (ChatViewPage gear location, ChatSettingsPanel gating + tuning
block, Mantine import state) are in `context.md` under "Feature-014 anchors".

Step-local facts:

- `ChatViewPage.tsx`: `const [settingsOpen, setSettingsOpen] = useState(false)`
  :26; gear `<ActionIcon ...><IconSettings size={18}/></ActionIcon>` :88-92; panel
  mount `<ChatSettingsPanel state opened onClose/>` :105. `Badge` is imported (:4)
  but only for the archived pill — add `Indicator` to the Mantine import; do not
  repurpose `Badge`. No gear animation/badge exists today.
- `ChatSettingsPanel.tsx`: `observer` :90; right `Drawer`. Gating: `isEditor` :102
  && `isChainMode = state.world?.generation_mode === "chain"` :103 → `showTuning`
  :104; tuning block :204-237. Component-local `useState`: `planTuning` /
  `toneTuning` / `tuningSaving` :97-99 (these are the accepted exception to
  no-useState — UI edit buffers). Lazy-load `useEffect` :118-122;
  reseed-from-profile `useEffect` :125-128 (extend this for the post-retune
  reseed). Textareas :212-229; Save `handleSaveTuning` :130-137; Revert
  `handleRevertTuning` :139-141. No Retune/Stop button, spinner, or disabled state
  today.
- The blink flag `retuneJustFinished`, the running flag `retuneRunning`, and the
  actions `triggerRetuneNow` / `stopRetune` / `clearRetuneBlink` are all defined in
  step 005 on `ChatPageState` — this step only consumes them.
- Mantine components available for the affordances: `Indicator` (gear blink,
  `processing` prop animates), `Loader` (spinner), `Button` (Retune now / Stop),
  Textarea `disabled` prop (grayed field). Match the existing panel's Mantine usage.
