# Step 005 — step-specific context

Feature-wide anchors (chatPageState observables/actions, api/chat.ts + tuningProfile.ts
surface, type files) are in `context.md` under "Feature-014 anchors". D1 (polling
transport) is in "Locked design decisions".

Step-local facts:

- `chatPageState.ts`: class `ChatPageState`, `makeAutoObservable` at :78.
  `tuningProfile: TuningProfile | null` :71. `streamCtrl: AbortController | null`
  :73 is non-observable (pattern to follow for the polling handle). Tuning actions:
  `applyTuningUpdate` :699 (REMOVE), `loadTuningProfile` :711 (KEEP),
  `saveTuningProfile` :727 (KEEP), `revertTuningProfile` :739 (KEEP). Existing stop
  primitive `stopGeneration(state)` :545 aborts `streamCtrl` — the retune stop is a
  SEPARATE action, don't overload it.
- `onTuningUpdate: (data) => applyTuningUpdate(state, data)` is wired into EVERY
  stream handler object at :349, :421, :504, :612 — remove all four.
- There is NO existing polling on the chat page. `setInterval` is used only in an
  unrelated admin page — mirror that interval idiom but ensure teardown on dispose
  (the chat page already has a dispose path where `stopRetunePolling` should hook
  in; the skeleton agent can locate it).
- `api/chat.ts`: `ChatSSEHandlers.onTuningUpdate?` :16; `_streamChat` switch
  `case "tuning_update"` :298-301. `regenerateMessage(chatId, handlers, turnNumber?,
  scope?, comment?)` :335 and `sendMessage` :327 are unchanged by this step.
- `api/tuningProfile.ts`: `getTuningProfile(worldId, signal?)` :4,
  `updateTuningProfile(worldId, body, signal?)` :8, both via shared `request<T>`
  from `./client` (`request<T>(url, {method, body, signal})`, non-2xx throws
  `ApiError`). Use the same `request<T>` in the new `api/retune.ts`.
- `types/tuningProfile.d.ts`: `TuningProfile {id, world_id, plan_tuning,
  tone_tuning}`, `UpdateTuningProfile {plan_tuning, tone_tuning}`. `RetuneStatus`
  overlaps in fields but is a distinct DTO (adds `running`, no `id`) — new file.
- `types/chat.d.ts:155-159`: `TuningUpdate {plan_tuning, tone_tuning, world_id}`
  (ambient) — remove.
- Edge detection: keep the PREVIOUS `retuneRunning` value before assigning the new
  one so the running→idle transition (true→false) can be detected in the same poll
  callback.
