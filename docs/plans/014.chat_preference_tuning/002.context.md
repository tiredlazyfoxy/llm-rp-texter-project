# Step 002 context

Chain pipeline map, placeholder mechanism, and default-template policy are in
`context.md`. Step-specific anchors:

## Exact touch sites (`chain_generation_service.py`)

- `_build_placeholder_values(context, chat, turn_facts, turn_decisions,
  tools_desc, decision="", user_instructions=None) -> dict[str,str]` ~:190-208
  — add `PLAN_TUNING` / `TONE_TUNING` to the returned map. The skeleton agent
  decides whether to add new params or read off threaded locals.
- `_run_chain_generation(...)` ~:622 — the run loop; load the profile here (it
  has `chat`, so `chat.user_id` / `chat.world_id` are available). The
  `DecisionState` is constructed here per tool stage (~:669) — the precedent for
  a per-run dynamic value, but tuning is loaded once and immutable.
- `_resolve_tool_prompt(...)` ~:211 and `_resolve_writer_prompt(...)` ~:237 call
  `_build_placeholder_values` then `resolve_prompt_template`.
- Entry fns: `generate_chain_response` ~:716, `regenerate_chain_response` ~:808
  both funnel into `_run_chain_generation`; loading inside the run loop covers
  both.

## Placeholder registry

`PLACEHOLDER_REGISTRY: list[PlaceholderInfo]` at
`backend/app/services/prompts/placeholder_registry.py:22-41`; `PlaceholderInfo`
TypedDict `{name, description, category}` at :16; `VALID_PLACEHOLDERS` derived
at :43. Existing dynamic names include `DECISION`, `USER_INSTRUCTIONS`,
`TURN_FACTS`, `TURN_DECISIONS` — match one of their categories.

## Resolver

`resolve_prompt_template(template, **values)` at
`backend/app/services/prompts/prompt_injection.py:23`; token regex
`\{([A-Z_]+)\}` at :43 — unknown tokens left as-is, so adding values is safe.

## Layer reminder

`chain_generation_service.py` is itself a service, so it loads the profile by
importing the step-001 db module directly:
`from app.db import tuning_profiles` then `await tuning_profiles.get(user_id,
world_id)`. Do NOT open sessions or use `select` here — that stays inside `db/`.
