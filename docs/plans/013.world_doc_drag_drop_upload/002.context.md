# Step 002 — Step-specific context

## What this step touches that no other step does

- `frontend/src/admin/pages/WorldViewPage.tsx::DocsTab` — only this
  step wraps its table area with drop-zone JSX.
- `frontend/src/admin/pages/worldViewPageState.ts` — only this step
  adds the new `dropActive` observable (or depth counter).
- *(Optional)* `frontend/src/admin/components/worlds/DocsTableDropZone.tsx`
  — only this step would create it, if extraction is chosen.

## Existing surfaces this step builds on (do not modify)

- `state.docTypeFilter` (computed in `worldViewPageState.ts:51-55`)
  — returns `"location"` / `"npc"` / `"lore_fact"` for typed tabs;
  `undefined` for `info` / `chats` / `all`. Gate the wrapper
  rendering on this being truthy.
- Module-level mutation `uploadDocuments(state, files, docType,
  signal)` in `worldViewPageState.ts:182-197` — reuse as-is.
  Already calls the API and refreshes the docs list; sets
  `state.docsError` on whole-batch failure.
- API client `uploadDocuments(worldId, files, docType, signal)` in
  `frontend/src/api/worlds.ts:117-131` — reused indirectly via the
  state mutation; not called directly from the component.
- Hidden file input + Upload menu in `DocsTab`
  (`WorldViewPage.tsx:328` and `:305-313`) — leave untouched. They
  share the same `uploadDocuments` mutation, so the two flows
  coexist cleanly.

## Accept-list parity

The hidden `<input>` uses `accept=".md,.txt" multiple` and
`uploadType` (`"location" | "npc"`). The drop-zone filter must
accept the same extensions. Add a brief comment near both the
input's `accept` attribute and the drop filter so future changes
move them together.

## Native HTML5 DnD gotchas

- **`preventDefault` is required on `dragover` AND `drop`.**
  Without it, the browser navigates to the file URL and the drop
  event silently does not fire on your handler.
- **`dragleave` fires on every descendant boundary crossing**, so
  toggling visual state on every event causes flicker. Use a depth
  counter (`++` on `dragenter`, `--` on `dragleave`, clear visual
  state at zero) or check `relatedTarget` against the wrapper's
  bounds. Counter approach is simpler.
- **`e.dataTransfer.files`** is a `FileList` (not an array). Use
  `Array.from(e.dataTransfer.files)`.
- **`e.dataTransfer.dropEffect = "copy"`** in `dragover` gives the
  user a clear cursor affordance; optional but nice.

The codebase has zero existing drag-drop precedent (harvester
confirmed: no `@mantine/dropzone` or `react-dropzone`, no
`onDrop` / `onDragOver` matches anywhere in `frontend/`). Do not
add a library — native DnD covers this.

## Visual feedback

Pick one (the simpler the better):

- A 2px dashed Mantine-blue border on the wrapper while
  `state.dropActive`.
- A semi-transparent overlay div with a "Drop files to upload"
  label.

Either is fine; match the project's existing Mantine visual
language. The overlay is friendlier; the border is smaller code.
Coder's call.

## Mixed-type rejection (whole drop)

Per the locked feature decision: **if any** dropped file has an
extension outside `.md` / `.txt`, reject the whole drop. No
partial upload — even the accepted files are not uploaded in
this case.

Logic:
- `const files = Array.from(e.dataTransfer.files);`
- `if (files.length === 0) return;` (no-op, no error)
- `if (files.some(f => !isAccepted(f))) { runInAction(() => { state.docsError = "Drop rejected: only .md and .txt files are accepted"; }); return; }`
- Otherwise call `uploadDocuments(state, files, state.docTypeFilter!, ctrl.signal)`.

`runInAction` is required for the error branch because it's a
synchronous handler boundary write to an observable. No API call
happens in the rejection branch.

## Refresh path is automatic

`uploadDocuments` (module-level mutation) already calls the docs
refresh after a successful API call. The drop handler does not
need its own refresh.

## AbortController

Use a fresh `AbortController` per drop, the same way the existing
`handleFileUpload` (line 261-267) does. Pass `ctrl.signal` to the
mutation. No need to retain the controller — drop is fire-and-
forget; the existing mutation handles its own try/catch.

## Out of scope for this step

- Backend changes (step 001).
- Per-file progress bars or per-file status reporting.
- Replacing or removing the existing Upload menu.
- Adding a "Upload Lore Facts" entry to the existing menu.
- Frontend test bootstrap — there is no test runner in
  `frontend/package.json`; verify via `npm run build` + manual
  smoke test, per Feature 012 step 005/006 precedent.
