# routes/

API route handlers — HTTP layer only: parse requests, call services, format responses. No business logic, no direct DB queries.

```
routes/
  auth.py, chat.py, llm_servers.py, admin/db_management.py, admin/pipelines.py, ...
```
